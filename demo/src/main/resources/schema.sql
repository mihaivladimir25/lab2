CREATE TABLE workout (
                         id BIGINT AUTO_INCREMENT PRIMARY KEY,
                         type VARCHAR(255),
                         duration INT,
                         calories INT,
                         date DATE
);